#!/bin/sh
set -eu

CLI=${DAYONE_READER_CLI:-"$HOME/.local/bin/dayone-reader"}

if [ ! -x "$CLI" ]; then
  echo "dayone-reader is not installed at $CLI. Install v2naix/dayone-reader or set DAYONE_READER_CLI to its absolute executable path." >&2
  exit 127
fi

exec "$CLI" "$@"
